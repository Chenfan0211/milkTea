.spec-sheet-root {
  position: fixed;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  z-index: 1200;
}

.spec-sheet__mask {
  position: absolute;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  background: rgba(0, 0, 0, 0.56);
}

.spec-sheet__panel {
  position: absolute;
  right: 0;
  bottom: 0;
  left: 0;
  display: flex;
  flex-direction: column;
  height: 88vh;
  overflow: hidden;
  background: var(--card-bg);
  border-radius: var(--radius-lg) var(--radius-lg) 0 0;
}

.spec-sheet__top-actions {
  position: absolute;
  top: 20rpx;
  right: 24rpx;
  z-index: 2;
  display: flex;
  gap: 16rpx;
}

.spec-sheet__circle-button {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 64rpx;
  height: 64rpx;
  background: var(--page-bg-neutral);
  border-radius: 50%;
}

.spec-sheet__circle-icon {
  width: 34rpx;
  height: 34rpx;
}

.spec-sheet__scroll {
  height: calc(88vh - 200rpx - env(safe-area-inset-bottom));
}

.spec-sheet__hero {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding-top: 20rpx;
}

.spec-sheet__hero-media {
  position: relative;
}

.spec-sheet__hero-image {
  width: 480rpx;
  height: 440rpx;
}

.spec-sheet__hero-badge {
  position: absolute;
  top: -4rpx;
  right: -4rpx;
  width: 34rpx;
  height: 34rpx;
}

.spec-sheet__image-note {
  margin-top: 8rpx;
  color: var(--text-muted);
  font-size: var(--font-caption);
  line-height: 28rpx;
}

.spec-sheet__content {
  padding: 0 var(--page-gutter) 40rpx;
}

.spec-sheet__content--edit {
  padding-top: 24rpx;
  padding-bottom: 24rpx;
}

.spec-sheet__name {
  display: block;
  color: var(--text-main);
  font-size: var(--font-xl);
  font-weight: 600;
  line-height: 54rpx;
}

.spec-sheet__name--edit {
  padding-right: 152rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.spec-sheet__promo-row {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 12rpx;
  margin-top: 20rpx;
}

.spec-sheet__promo,
.spec-sheet__feature-tag {
  padding: 4rpx 8rpx;
  font-size: var(--font-sm);
  line-height: 30rpx;
  border-radius: var(--radius-xs);
}

.spec-sheet__promo {
  color: var(--brand-green);
  border: 2rpx solid var(--brand-green);
}

.spec-sheet__feature-tag {
  color: var(--brand-green);
  background: var(--brand-soft);
}

.spec-sheet__price-label {
  color: var(--text-secondary);
  font-size: var(--font-sm);
}

.spec-sheet__price-symbol,
.spec-sheet__start-price {
  color: var(--text-main);
  font-weight: 600;
}

.spec-sheet__price-symbol {
  font-size: var(--font-base);
}

.spec-sheet__start-price {
  font-size: var(--font-lg);
}

.spec-sheet__description {
  display: block;
  margin-top: 20rpx;
  color: var(--text-main);
  font-size: var(--font-base);
  line-height: 42rpx;
}

.spec-sheet__description--secondary {
  margin-top: 0;
}

.spec-sheet__info-block {
  margin-top: 24rpx;
}

.spec-sheet__info-title,
.spec-sheet__info-text {
  display: block;
  color: var(--text-secondary);
  font-size: var(--font-base);
  line-height: 42rpx;
}

.spec-sheet__expand {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 72rpx;
  color: var(--text-main);
  font-size: var(--font-base);
}

.spec-sheet__expand-icon {
  width: 28rpx;
  height: 28rpx;
  margin-left: 8rpx;
  transition: transform 0.2s;
}

.spec-sheet__expand-icon.is-expanded {
  transform: rotate(180deg);
}

.spec-group {
  margin-top: 24rpx;
}

.spec-group__label {
  display: block;
  margin-bottom: 12rpx;
  color: var(--text-main);
  font-size: var(--font-base);
}

.spec-group__options {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
}

.spec-option {
  display: flex;
  align-items: center;
  min-height: 64rpx;
  padding: 0 20rpx;
  color: var(--text-main);
  font-size: var(--font-base);
  background: var(--page-bg-neutral);
  border-radius: var(--radius-sm);
}

.spec-option.is-selected {
  color: #FFFFFF;
  background: var(--brand-green);
}

.spec-option__icon {
  width: 30rpx;
  height: 30rpx;
  margin-right: 8rpx;
}

.spec-option__delta {
  margin-left: 8rpx;
  color: var(--price-red);
}

.spec-option.is-selected .spec-option__delta {
  color: #FFFFFF;
}
.spec-sheet__action {
  flex: none;
  padding: 16rpx var(--page-gutter) calc(16rpx + env(safe-area-inset-bottom));
  background: var(--card-bg);
  border-top: 1rpx solid var(--line-color);
}

.spec-sheet__summary {
  display: flex;
  align-items: center;
  min-height: 96rpx;
}

.spec-sheet__summary-info {
  flex: 1;
  min-width: 0;
}

.spec-sheet__summary-line {
  display: flex;
  align-items: center;
}

.spec-sheet__summary-price {
  display: flex;
  align-items: baseline;
  min-width: 0;
}

.spec-sheet__member-symbol {
  color: var(--text-main);
  font-size: var(--font-sm);
  font-weight: 600;
}

.spec-sheet__member-price {
  color: var(--text-main);
  font-size: var(--font-xl);
  font-weight: 600;
}

.spec-sheet__summary-original {
  margin-left: 12rpx;
  color: var(--text-muted);
  font-size: var(--font-sm);
  text-decoration: line-through;
}

.spec-sheet__favorite {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 44rpx;
  height: 44rpx;
  margin-left: 12rpx;
}

.spec-sheet__favorite-icon {
  width: 34rpx;
  height: 34rpx;
}

.spec-sheet__favorite--inline {
  width: 40rpx;
  height: 40rpx;
  margin-right: 4rpx;
  margin-left: 0;
}

.spec-sheet__spec-text {
  display: block;
  flex: 1;
  min-width: 0;
  margin-top: 4rpx;
  overflow: hidden;
  color: var(--text-secondary);
  font-size: var(--font-sm);
  text-overflow: ellipsis;
  white-space: nowrap;
}

.spec-sheet__spec-line {
  display: flex;
  align-items: center;
  min-width: 0;
}

.spec-sheet__spec-line .spec-sheet__spec-text {
  margin-top: 4rpx;
}

.spec-sheet__stepper {
  display: flex;
  flex: none;
  align-items: center;
  height: 64rpx;
  margin-left: 24rpx;
  overflow: hidden;
  border: 1rpx solid var(--line-color);
  border-radius: var(--radius-sm);
}

.spec-sheet__step {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 64rpx;
  height: 64rpx;
}

.spec-sheet__step--minus.is-disabled {
  opacity: 0.35;
}

.spec-sheet__step--plus {
  background: var(--brand-green);
}

.spec-sheet__step-icon {
  width: 34rpx;
  height: 34rpx;
}

.spec-sheet__quantity {
  width: 64rpx;
  color: var(--text-main);
  font-size: var(--font-base);
  text-align: center;
}

.spec-sheet__buttons {
  display: flex;
  gap: 20rpx;
  margin-top: 16rpx;
}

.spec-sheet__button {
  display: flex;
  flex: 1;
  align-items: center;
  justify-content: center;
  height: 80rpx;
  font-size: var(--font-lg);
  font-weight: 600;
  border-radius: var(--radius-pill);
}

.spec-sheet__button--secondary {
  color: var(--brand-green);
  background: var(--card-bg);
  border: 2rpx solid var(--brand-green);
}

.spec-sheet__button--primary {
  color: #FFFFFF;
  background: var(--brand-green);
}

.spec-sheet__button--confirm {
  flex: none;
  width: 100%;
  margin-top: 16rpx;
  color: #FFFFFF;
  background: var(--brand-green);
}

.spec-sheet__taste-tip {
  position: absolute;
  top: 50%;
  left: 50%;
  z-index: 3;
  display: flex;
  align-items: center;
  padding: 16rpx 24rpx;
  color: #FFFFFF;
  font-size: var(--font-base);
  background: rgba(0, 0, 0, 0.68);
  border-radius: var(--radius-pill);
  transform: translate(-50%, -50%);
}

.spec-sheet__taste-icon {
  width: 28rpx;
  height: 28rpx;
  margin-left: 8rpx;
}
