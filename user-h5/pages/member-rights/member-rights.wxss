.member-rights-page {
  display: flex;
  flex-direction: column;
  width: 100%;
  height: 100vh;
  overflow: hidden;
  background: var(--page-bg-neutral);
}

.member-rights-scroll {
  flex: 1;
  min-height: 0;
}

.member-rights-content {
  padding: 0 0 calc(40rpx + env(safe-area-inset-bottom));
}
