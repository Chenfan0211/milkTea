<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!--
      交易服务（第 7 期拆分）。

      职责：订单、支付、核销、退款、订单超时。

      依赖说明：
        - 通过 ProductQueryPort 访问 product/subject 数据，
          远程实现走 server 的 /internal/** 只读接口（不经网关）；
        - 通过 MQ 事件与 finance / marketing 交互（第 5、6 期已解耦）；
        - 退款前置校验走 server 的 /internal/settlement-status（同步，防资金穿透）。
    -->
    <parent>
        <groupId>com.wuling</groupId>
        <artifactId>wuling-parent</artifactId>
        <version>0.1.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>trade-service</artifactId>
    <name>wuling-trade-service</name>
    <description>五零时光交易服务（订单 / 支付 / 核销 / 退款）</description>

    <dependencies>
        <dependency>
            <groupId>com.wuling</groupId>
            <artifactId>common</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.wuling</groupId>
            <artifactId>security-common</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-amqp</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>

        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
            <version>${mybatis-plus.version}</version>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-loadbalancer</artifactId>
        </dependency>
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
        </dependency>

        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!--
          微信支付 APIv3 官方 SDK（第 14 期支付接入）。

          为什么放在 trade-service 而不是 wuling-common：
          只有交易服务需要发起支付与验签回调；放到公共模块会把 okhttp/gson
          传递进 gateway（WebFlux）等无关服务，扩大依赖面与冲突概率。

          注意：本依赖即使 PAY_CHANNEL=mock 也存在于 classpath，
          但相关 Bean 由 WechatPayConfig 按配置条件装配，
          mock 通道下不会实例化，不影响现有链路。
        -->
        <dependency>
            <groupId>com.github.wechatpay-apiv3</groupId>
            <artifactId>wechatpay-java</artifactId>
            <version>${wechatpay-java.version}</version>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <!--
      ============================================================
      构建习惯（踩坑固化）：wuling-common 改动后必须重新 install
      ============================================================

      事故回顾：wuling-common 里给 OrderVerifiedEvent.Line 新增了 4 参数构造器，
      但本地仓库的 common-0.1.0-SNAPSHOT.jar 仍是 8 小时前的旧包，
      导致 trade-service 编译报「找不到合适的构造器」。

      排查成本高的原因：错误信息指向 VerifyService（业务代码），
      而病根在构建产物 —— 表现为「代码明明是对的，编译就是不过」。
      更隐蔽的是 artifactId 叫 common 而模块目录叫 wuling-common，
      做增量编译（-pl trade-service）时不会带上它。

      处置：
        · 改 wuling-common / security-common 后，用
          `mvn -pl wuling-common,security-common -am install -DskipTests`
          让依赖模块拿到最新类；
        · 下面的 maven-enforcer 规则会校验「被依赖模块是否已安装」，
          未安装时直接失败并提示命令，而不是抛出难懂的编译错误。
    -->
    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>repackage</id>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
