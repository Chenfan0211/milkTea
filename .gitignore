# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.DS_Store
dist
dist-ssr
coverage
*.local

/cypress/videos/
/cypress/screenshots/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/launch.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

package-lock.json
yarn.lock

.VSCodeCounter
src-tauri/target
.codegraph

# backend
# Maven 构建产物（各模块通用）
**/target/
!**/target/.gitkeep
server/*.log


# backend secrets (不要提交)
server/src/main/resources/application-local.yml
server/src/main/resources/application-prod.yml

# 部署实际口令（模板见 deploy/dev/.env.example）
deploy/dev/.env
deploy/**/.env

# 构建产物与临时文件
dist.tar.gz
.tmp-*

# 本地开发密钥（口令、token），禁止提交
.env.local.properties
server/.env.local.properties


# 排查/联调临时脚本（不入库）
.tmp_*
.tmp/
fix_*.py
gen_*.py
patch-*.cjs

# 部署产物 / 构建备份（本地打包生成，不入库）
web-ip-*.tar.gz
backend-*.tar.gz
*.tar.gz


# 临时 IP 直连构建配置（含生产 IP，不入库）
.env.prod-ip


# 部署暂存目录（含编译产物 jar，不入库）
.deploy-staging/


# Codex 日志/临时排查记录（不入库）
.codex-logs/
