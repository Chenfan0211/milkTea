-- =============================================================
-- 修正：订单明细的规格快照为展示文案（如 "[中杯,标准冰],加马蹄粉圆"），
-- 不是合法 JSON，原 JSON 列会导致写入报错，改为 VARCHAR。
-- =============================================================
ALTER TABLE order_item MODIFY COLUMN spec_snapshot VARCHAR(255) NULL COMMENT '规格快照文案';
