spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:13306/wuling?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true&rewriteBatchedStatements=true
    username: ${MYSQL_USER:wuling}
    password: ${MYSQL_PASSWORD:wuling123}
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: ${REDIS_HOST:127.0.0.1}
      port: ${REDIS_PORT:16379}
      password: ${REDIS_PASSWORD:wuling123}
      timeout: 3000ms
  rabbitmq:
    # 本地开发经 SSH 隧道访问：本地 15672 -> 服务器 AMQP 5672
    # （生产环境直连 127.0.0.1:5672，见 application-prod.yml）
    host: ${RABBITMQ_HOST:127.0.0.1}
    port: ${RABBITMQ_PORT:15672}
    username: ${RABBITMQ_USER:wuling}
    password: ${RABBITMQ_PASSWORD:wuling123}

management:
  endpoints:
    web:
      exposure:
        include: health,info

